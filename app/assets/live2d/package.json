{
  "private": true,
  "scripts": {
    "build": "vite build && node copy_resources.js",
    "serve": "vite preview --port 5000 --host",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^5.9.3",
    "vite": "^8.0.2"
  }
}
